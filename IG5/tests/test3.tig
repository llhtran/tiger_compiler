/* a record type and a record variable */
let
	type  rectype = {name:string, age:int}
	var rec1:rectype := rectype {name="Nobody", age=300}
in
	rec1.name := "Somebody";
	print(rec1.name);
	if rec1.age=300 then print("amamazing")
end
